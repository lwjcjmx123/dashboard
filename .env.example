# Database Configuration
# If you have a database, set DATABASE_URL or MYSQL_URL
# If not set, the app will use IndexedDB for client-side storage

# DATABASE_URL="mysql://username:password@host:port/database"
# MYSQL_URL="mysql://username:password@host:port/database"

# Force IndexedDB usage (useful for testing)
# FORCE_INDEXEDDB=true

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000